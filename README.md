# My OS — 개인 생산성 앱

개인 운영체제 컨셉의 올인원 생산성 앱. 단일 HTML 파일 프론트엔드 + Vercel 서버리스 API + Supabase DB로 구성.

---

## 기술 스택

| 영역 | 기술 |
|------|------|
| 프론트엔드 | `public/index.html` (CSS/JS 인라인, 단일 파일) |
| 백엔드 | Vercel Serverless Functions (`api/*.js`) |
| 데이터베이스 | Supabase (PostgreSQL + Row Level Security) |
| 인증 | Supabase Auth (Google OAuth) |
| 배포 | Vercel |

---

## 구현된 기능

### 인증
- Google OAuth 로그인 (Supabase Auth)
- JWT 토큰 기반 API 인증
- 게스트 모드 지원

### 할 일 (Tasks)
- 폴더별 작업 분류
- 체크리스트 토글
- 드래그 앤 드롭 정렬
- 서버 동기화

### 습관 트래커 (Habits)
- 습관 추가/삭제
- 최근 7일 체크 셀 (Vine / Lego / Slime 블록 디자인 선택 가능)
- 스트릭 카운트
- 이모티콘 커스터마이징 (클릭으로 변경)
- **AI 자동 추천**: 습관 추가 시 이모티콘 + 블록 디자인 AI 자동 설정
- 블록 디자인 선택 UI (각 습관마다 개별 설정, 서버 저장)

### 목표 (Goals)
- 목표 생성 및 진행률 관리
- 대시보드 마인드맵 뷰

### Time Tracker
- 60블록 시간 시각화
- 시간대별 색상 구분
- **모바일 글래스모피즘 사이드바**: 라벨 패널이 슬라이드 드로어로 전환, backdrop-filter blur 효과
- 사이드바 터치 드래그로 너비 조절 가능

### 포모도로
- SVG 링 타이머
- 세션 카운트
- **설정 서버 저장**: 집중/휴식 시간 새로고침 후에도 유지

### 루틴
- 아침/점심/저녁 루틴 관리
- 요일별 설정, 알람 시간 설정
- **서버 저장**: 새로고침 후에도 루틴 데이터 유지

### 메모
- 자유 텍스트 메모
- 서버 저장

### 이벤트 / 캘린더
- 일정 추가 및 날짜별 뷰

### AI 어시스턴트
- 3가지 모드 (일반 / 집중 / 창의)
- Vercel AI API 연동

### 커뮤니티 (Twitter/X 스타일)
- 전체 유저 공유 피드
- 게시글 작성 (280자 제한, SVG 원형 카운터)
- 좋아요 토글
- 댓글 추가/삭제
- 게시글 삭제 (본인 것만)
- Supabase `community_posts` 테이블에 실시간 동기화

---

## API 엔드포인트

| 파일 | 역할 |
|------|------|
| `api/tasks.js` | 할 일 CRUD |
| `api/habits.js` | 습관 CRUD + 체크 |
| `api/goals.js` | 목표 CRUD |
| `api/events.js` | 이벤트 CRUD |
| `api/folders.js` | 폴더 관리 |
| `api/profile.js` | 프로필 설정 저장 |
| `api/community.js` | 커뮤니티 피드 (GET/POST/PATCH/DELETE) |
| `api/ai.js` | AI 메시지 처리 |

---

## DB 스키마

- `schema_v3.sql` — 메인 테이블 (tasks, habits, goals, events 등)
- `schema_community.sql` — 커뮤니티 게시글 테이블

> Supabase SQL Editor에서 순서대로 실행 필요

---

## 로컬 개발

```bash
npm install
npm run dev   # vercel dev → localhost:3000
```

## 배포

```bash
npm run deploy   # vercel --prod
```

---

## 핵심 설계 원칙

> 각 유저의 개인화 OS를 통한 개인화된 삶을 우선시합니다.
> 모든 설정·데이터는 계정별로 서버에 영구 저장됩니다.

---

## AI 호출 정책

| 호출 위치 | 빈도 | 캐시 |
|-----------|------|------|
| 할일 생성 시 태그 자동 분류 | 태스크당 1회 | 없음 |
| 홈 AI 코치 메시지 | 10분 쿨다운 | 슬롯 기반 캐시 |
| AI 배지 생성 | 1시간 쿨다운 | 있음 |
| 습관 추가 시 이모티콘+스타일 추천 | 습관당 1회 | 없음 |

---

## 주요 작업 이력

| 날짜 | 내용 |
|------|------|
| 초기 구축 | Google OAuth + 서버 스토리지 마이그레이션 (localStorage 제거) |
| 기능 추가 | 홈 위젯 · Time Tracker · 메모 · 포모도로 전체 구현 |
| 버그 수정 | 체크리스트 토글 · 드래그 충돌 · 습관 3열 레이아웃 |
| 개선 | 습관 90일 GitHub 히트맵 도입 (이후 7일 셀로 단순화) |
| 모바일 | 반응형 레이아웃 구현 |
| 커뮤니티 | Supabase 공유 피드 서버 연동 |
| 커뮤니티 리디자인 | Twitter/X 스타일 UI, SVG 아이콘, 원형 글자 수 카운터 |
| 버그 수정 | 게시글 업로드 후 사라짐 현상 수정 (임시 포스트 유지 처리) |
| 2026-05 | **모바일 전반 개선** — 안전 영역, 스크롤 방지, + 버튼 오버플로 수정 |
| 2026-05 | **데이터 저장 버그 수정** — routines, habitEmojis, pomodoroPresets 누락 해결 |
| 2026-05 | **할일 그룹 순서 설정** — 언제든/오늘/기한초과/예정 순서 드래그 변경 가능 |
| 2026-05 | **시작 화면 설정** — 새로고침 후 마지막 화면 유지 vs 할일 목록 선택 |
| 2026-05 | **타임 트래커 모바일 사이드바** — 글래스모피즘 슬라이드 드로어, 터치 리사이즈 |
| 2026-05 | **습관 AI 자동 추천** — 습관 추가 시 이모티콘 + 블록 디자인 자동 설정 |
| 2026-05 | **핀치 줌 방지** — 모바일 확대/축소 차단 |
